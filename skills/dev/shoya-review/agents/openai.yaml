interface:
  display_name: "Shoya Review"
  short_description: "Question intent and verify changes end to end"
  default_prompt: "Use $shoya-review to independently scrutinize this change end to end, including its intent, simpler alternatives, real code paths, and claims."
