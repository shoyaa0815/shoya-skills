interface:
  display_name: "Shoya Build"
  short_description: "Build approved changes with traceable verification"
  default_prompt: "Use $shoya-build to implement this approved change and verify it against its acceptance criteria."
