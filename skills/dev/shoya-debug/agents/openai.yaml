interface:
  display_name: "Shoya Debug"
  short_description: "Prove root causes before fixing failures"
  default_prompt: "Use $shoya-debug to reproduce this failure, prove its root cause, and fix it only if authorized."
