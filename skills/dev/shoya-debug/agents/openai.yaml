interface:
  display_name: "Shoya Debug"
  short_description: "Find and fix root causes with minimal evidence"
  default_prompt: "Use $shoya-debug to diagnose this failure with the smallest decisive evidence loop, then fix and verify it if authorized."
